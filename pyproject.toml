[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sulp"
version = "1.0-RC"
dependencies = ["numpy", "torch >= 2.8"]
requires-python = ">= 3.8"

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
pythonpath = ["src"]
