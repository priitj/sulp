[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sulp"
version = "1.0-RC"

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
pythonpath = ["src"]

